# Later

Ideas deliberately out of scope for v0.1 (see spec non-goals):

- macOS (Quartz/Accessibility) and Linux (X11/Wayland) injection backends
- Spoken punctuation/commands ("comma", "new line", "scratch that")
- Streaming partial-hypothesis overlay UI while speaking
- Stutter/duplicate-word dedup ("the the") in the rule polisher
- Cloud STT engine behind the same TranscriptionEngine protocol
- PyInstaller one-file packaging + signed installer, auto-start on login
- GUI settings window and dictionary editor
- Per-application injection profiles (e.g. markdown mode in editors)
